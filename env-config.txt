# Environment Configuration for TaskFlow Client
# Copy this content to your .env file

# Development Server Configuration
VITE_DEV_SERVER_PORT=3000

# Primary API Configuration (Port 5041)
# Local development
VITE_API_BASE_URL=http://localhost:5041
# Deployed backend (replace with your actual deployed URL)
VITE_PROD_API_BASE_URL=https://your-deployed-backend-5041.com
VITE_API_TIMEOUT=10000

# Secondary API Configuration (Port 7029)
# Local development
VITE_SECONDARY_API_BASE_URL=http://localhost:7029
# Deployed backend (replace with your actual deployed URL)
VITE_PROD_SECONDARY_API_BASE_URL=https://your-deployed-backend-7029.com
VITE_SECONDARY_API_TIMEOUT=10000

# SignalR Configuration (Port 5041)
# Local development
VITE_SIGNALR_HUB_URL=http://localhost:5041/taskHub
# Deployed backend (replace with your actual deployed URL)
VITE_PROD_SIGNALR_HUB_URL=https://your-deployed-backend-5041.com/taskHub
VITE_SIGNALR_RECONNECT_INTERVAL=5000
VITE_SIGNALR_MAX_RECONNECT_ATTEMPTS=5

# Secondary SignalR Configuration (Port 7029)
# Local development
VITE_SECONDARY_SIGNALR_HUB_URL=http://localhost:7029/taskHub
# Deployed backend (replace with your actual deployed URL)
VITE_PROD_SECONDARY_SIGNALR_HUB_URL=https://your-deployed-backend-7029.com/taskHub

# GitHub OAuth Configuration
VITE_GITHUB_CLIENT_ID=
VITE_GITHUB_REDIRECT_URI=http://localhost:3000/github/callback

# Feature Flags
VITE_ENABLE_DEBUG_LOGS=true
VITE_ENABLE_SIGNALR=true
VITE_ENABLE_SECONDARY_API=true

# Storage Keys
VITE_ACCESS_TOKEN_KEY=accessToken
VITE_REMEMBER_ME_KEY=rememberMe

# Environment Mode
# Set to 'development' for localhost, 'production' for deployed URLs
NODE_ENV=development 